import tailwindcss from '@tailwindcss/postcss';

export default {
  plugins: [
    tailwindcss,
  ],
};
// This configuration file sets up PostCSS to use Tailwind CSS as a plugin.
// It imports the Tailwind CSS plugin and exports a configuration object that includes the plugin in the `plugins` array.   